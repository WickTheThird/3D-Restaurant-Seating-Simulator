export const DEFAULT_RECTANGULAR_TABLE_DIMENSIONS = {
    height: 0.75, // Default height in meters
    width: 1.5    // Default width in meters
};

export const DEFAULT_ROUND_TABLE_DIMENSIONS = {
    radius: 0.75  // Default radius in meters
};

export const DEFAULT_SEAT_DIMENSIONS = {
    height: 0.45, // Default height in meters
    width: 0.5    // Default width in meters
};

export const DEFAULT_PERSON_HEIGHT = 1.8; // Default height for a person in meters

export const DEFAULT_ROOM_DIMENSIONS = {
    length: 5,   // Default length of the room in meters
    width: 4,    // Default width of the room in meters
    height: 3    // Default height of the room in meters
};